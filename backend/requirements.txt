fastapi==0.115.0
uvicorn[standard]==0.32.0
# 2.0.35 predates CPython 3.14 and crashes at import time on any model with
# a stringified union annotation (e.g. Mapped["Credential | None"]) with
# `TypeError: descriptor '__getitem__' requires a 'typing.Union' object but
# received a 'tuple'` - a real incompatibility with 3.14's typing changes,
# not a packaging issue. Fixed as of 2.0.52 (reproduced/confirmed against a
# real Python 3.14 interpreter, not inferred from release notes).
sqlalchemy==2.0.52
# Required by SQLAlchemy's async engine. Normally pulled in transitively via
# an environment marker on supported platforms, but pin it explicitly - a
# partially-failed `pip install` can leave it (and only it) missing, which
# then only surfaces as a confusing runtime ImportError from deep inside
# SQLAlchemy instead of a clear install-time failure.
greenlet==3.5.5
# 0.30.0/2.9.9 predate CPython 3.14 and have no wheel for it, forcing a
# from-source build that then fails without PostgreSQL's pg_config
# installed. These versions add cp314 wheels.
asyncpg==0.31.0
aiosqlite==0.20.0
psycopg2-binary==2.9.12
alembic==1.13.3
# pydantic==2.9.2 pins pydantic-core==2.23.4, a compiled Rust extension with
# no CPython 3.14 wheel - pip then tries to compile it from source, which
# needs a Rust toolchain AND the MSVC linker (Visual C++ Build Tools), which
# most machines don't have. This version's pydantic-core has a cp314 wheel.
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.2.0
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.12
cryptography==43.0.1
netmiko==4.4.0
celery==5.4.0
redis==5.1.1
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
